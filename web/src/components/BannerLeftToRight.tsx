import React from "react";
import ParagraphSmall from "./typography/ParagraphSmall";

const LeftToRightBanner: React.FC = () => {
  const textContent =
    "WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED * WRAP YOUR DATA * CHAT WRAPPED *";
  return (
    <div className="w-screen overflow-hidden">
      <style>
        {`
          @keyframes marquee {
            from { transform: translateX(0); }
            to { transform: translateX(-33.3333%); }
          }

          @keyframes marquee-reverse {
            from { transform: translateX(-33.3333%); }
            to { transform: translateX(0); }
          }

          .marquee-container {
            display: flex;
            white-space: nowrap;
            width: 300%;
          }

          .animate-marquee {
            animation: marquee 10s linear infinite;
          }

          .animate-marquee-reverse {
            animation: marquee-reverse 10s linear infinite;
          }
        `}
      </style>

      {/* Right to Left */}
      <div className="w-full bg-gradient-to-r from-[#0D1B2A] to-[#1B263B] py-3 overflow-hidden">
        <div className="marquee-container animate-marquee">
          {[...Array(3)].map((_, i) => (
            <span key={i} className="text-white font-mono min-w-screen p-5">
              <p className="text-paragraphsmall font-thin">{textContent}</p>
            </span>
          ))}
        </div>
      </div>
    </div>
  );
};

export default LeftToRightBanner;
